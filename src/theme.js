import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
        light: "#ff9574",
        main: "#ff6347",
    },

    secondary: {
      main: "#ff9574",
    },
  },
});

export default theme;
